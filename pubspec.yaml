name: expense_tracker_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1
  cloud_firestore: ^6.9.0
  flutter_bloc: ^9.1.1
  shared_preferences: ^2.5.5
  top_snackbar_flutter: ^3.4.0
  intl: ^0.20.3
  fl_chart: ^1.2.0
  get_it: ^8.3.0
  injectable: ^3.0.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  go_router: ^14.0.0
  flutter_native_splash: ^2.4.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4  # Add this line here

  injectable_generator: ^3.1.3
  build_runner: ^2.16.1
  flutter_lints: ^6.0.0
  freezed: ^4.0.1
  json_serializable: ^6.14.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/logo.png


flutter_native_splash:
  color: "#9575CD"      # Softer, modern Pale Purple (Matches your app gradient tint)
  color_dark: "#9575CD" # Same pale purple for a uniform smooth transition

  image: images/logo.png
  image_dark: images/logo.png

  fullscreen: true

  android: true
  ios: true

  android_12:
    color: "#9575CD"
    color_dark: "#9575CD"

    image: images/logo.png
    image_dark: images/logo.png


flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/logo.png"

  # Adaptive Icon properties for modern Android devices (Android 8.0+)
  adaptive_icon_background: "#9575CD"  # Fills the background with your Pale Purple tint
  adaptive_icon_foreground: "images/logo.png" # Layers your transparent logo graphic on top

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
